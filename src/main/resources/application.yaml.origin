spring:
  liquibase:
    enabled:
  datasource:
    url: jdbc:h2:mem:mydb
    username:
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true